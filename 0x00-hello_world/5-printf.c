/*
 * File: 5-printf.c
 * a program that prints a statement with a new line
 */

#include <stdio.h>

/**
 * main - Prints "with proper grammar, but the outcome is
 *                a piece of art," followed by a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
